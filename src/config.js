export const API = {
  // NOTE: hostname provided by Vite. When it's hosted online, the hostname will be the same as the server.
  // url: process.env.API_URL || 'http://localhost:3000',
  url: '/api',
}
